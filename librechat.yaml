# Configuration version (required)
version: 1.0.2

# Cache settings: Set to true to enable caching
cache: true

# Example Registration Object Structure (optional)
registration:
  socialLogins: ["github", "google", "discord", "openid", "facebook"]
  # allowedDomains:
    # - "gmail.com"

# Definition of custom endpoints
endpoints:
  custom:
    # Rivet
    - name: "Rivet"
      apiKey: "80f946e4-04ba-4687-8c9e-bd33337f0f90"
      baseURL: "https://rivet-chat-api-production-77f2.up.railway.app"
      models:
        default: ["example.rivet-project"]
        fetch: true
      titleConvo: true
      titleModel: "example.rivet-project"
      summarize: false
      summaryModel: "example.rivet-project"
      forcePrompt: false
      modelDisplayLabel: "Rivet"
      addParams:
        safe_prompt: true
